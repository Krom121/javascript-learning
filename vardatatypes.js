// Variables and data types

var firstName = 'John';
console.log(firstName);

var lastName = 'Smith';
var age = 28;

var fullAge = true;
console.log(fullAge);

var job;
console.log(job);

job = 'Teacher';
console.log(job);

// What be put in a variable

var _3years = 3;
var johnMark = 'John and Mark';


// Variable Mutation and Type Coerion.

var firstName = 'John';
var age = 28;
console.log(firstName + ' ' + age);

// Declare many variables on one line define later.

var job, ismarried;
job = 'teacher';
ismarried = false;

// Type Coercion

console.log(firstName + ' is a ' + age + ' year old ' + job + '. Is he married?' + ismarried);

// Variable Mutation.

age = 'twenty eight';
job = 'driver';

alert(firstName + ' is a ' + age + ' year old ' + job + '. Is he married?' + ismarried);

var lastName = prompt('What is his last name?');
console.log(firstName + ' ' + lastName);